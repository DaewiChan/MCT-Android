<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MCT"
        tools:targetApi="31">
        <activity
            android:name=".presentation.splash.SplashScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.MCT.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.account.forgot_password.ForgotPasswordActivity"
            android:exported="false" />
        <activity
            android:name=".presentation.account.login.LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.MCT.NoActionBar">

        </activity>
        <activity
            android:name=".presentation.account.register.RegisterActivity"
            android:exported="true"
            android:theme="@style/Theme.MCT.NoActionBar" />
        <activity
            android:name=".presentation.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MCT.NoActionBar" />
    </application>

</manifest>